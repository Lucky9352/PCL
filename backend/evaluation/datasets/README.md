# Benchmark datasets (optional)

Place downloaded files here. See [docs/DATASETS.md](../../../docs/DATASETS.md), [docs/EVALUATION.md](../../../docs/EVALUATION.md), and [docs/PARAMETER_PROVENANCE.md](../../../docs/PARAMETER_PROVENANCE.md).

Expected filenames for `python -m evaluation.run_all`:

- `babe.tsv`
- `liar_test.tsv` or `liar_train.tsv`
- `clef_checkworthy.csv`

This directory is intentionally empty in git.
