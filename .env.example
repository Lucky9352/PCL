# ═══════════════════════════════════════════════════
# IndiaGround — Environment Configuration
# ═══════════════════════════════════════════════════
# Copy to .env and fill in values:  cp .env.example .env

# Frontend
VITE_API_URL=http://localhost:8000

# FastAPI / Backend
APP_ENV=development
DEBUG=1
SECRET_KEY=5GbP-ssS8uSrOL_i-wsjuQzrB8UGkavKAXgyEx1k-zTomXs0apiZ9ZicmsdTul2vpYVGoSmP6s1qdVSkM0-HxA

# Database (PostgreSQL)
# Local docker-compose PostgreSQL:
DATABASE_URL=postgresql+asyncpg://indiaground:indiaground@localhost:5432/indiaground

# Sync URL for Alembic migrations:
DATABASE_URL_SYNC=postgresql://indiaground:indiaground@localhost:5432/indiaground

# Redis / Celery
REDIS_URL=redis://127.0.0.1:6379/0
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/0

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ClaimBuster API (optional — graceful fallback if empty)
CLAIMBUSTER_API_KEY=

# SerpAPI (optional — uses DuckDuckGo if empty)
SERPAPI_KEY=

# Google Fact Check Tools API (optional)
GOOGLE_FACTCHECK_API_KEY=

# Scraper
# Scrape interval in minutes (default: 30)
SCRAPE_INTERVAL_MINUTES=30

# ML / NLP
# Device override: auto | cpu | cuda | mps
# "auto" detects best available (CUDA GPU > Apple MPS > CPU)
ML_DEVICE=auto

# HuggingFace cache dir (optional, defaults to ~/.cache/huggingface)
# HF_HOME=/path/to/cache
